# Base de datos PostgreSQL
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=river123
DB_DATABASE=receptionistai
POSTGRES_USER=postgres
POSTGRES_PASSWORD=river123
POSTGRES_DB=receptionistai

# OpenAI API
OPENAI_API_KEY=tu_openai_api_key_aqui

# Google APIs (OAuth)
GOOGLE_CLIENT_ID=tu_google_client_id_aqui
GOOGLE_CLIENT_SECRET=tu_google_client_secret_aqui
# Redirect URI para login de usuarios con Google
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback
# Redirect URI específico para Google Calendar (opcional)
# Si no se configura, se construye automáticamente desde GOOGLE_REDIRECT_URI o BACKEND_URL
# GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:3001/google-calendar/auth/callback

# Google Service Account (para integración directa)
GOOGLE_CLIENT_EMAIL=tu_service_account_email@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\ntu_private_key_aqui\n-----END PRIVATE KEY-----\n"

# Google Calendar
GOOGLE_CALENDAR_ID=primary

# Google Sheets
GOOGLE_SHEETS_ID=tu_google_sheets_id_aqui
GOOGLE_SHEETS_RANGE=A:J

# Puerto del servidor
PORT=3001

# Backend URL (para webhooks)
BACKEND_URL=http://localhost:3001

# Webhook URLs (para ngrok - solo la base URL, el path se construye automáticamente)
# URL para n8n
WEBHOOK_URL_N8N=https://tu-ngrok-n8n-url.com
# URL para backend (usado por ElevenLabs tools)
WEBHOOK_URL_BACKEND=https://tu-ngrok-backend-url.com

# Entorno
NODE_ENV=development

# Vapi AI - Para asistentes de voz
VAPI_API_KEY=tu_vapi_api_key_aqui
VAPI_PUBLIC_KEY=tu_vapi_public_key_aqui

# SMTP Configuration (para envío de emails)
# Para desarrollo puedes usar Ethereal Email: https://ethereal.email
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=tu_smtp_user_aqui
SMTP_PASSWORD=tu_smtp_password_aqui

# Database SSL
DB_SSL_ENABLED=false